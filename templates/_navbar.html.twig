{% set currentRoute = app.request.attributes.get('_route') %}
<nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_default_index') }}">Sensio TV+</a>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
                <a class="nav-link{{ currentRoute == 'app_default_index' ? ' active' : '' }}" href="{{ path('app_default_index') }}">Home</a>
            </li>
            <li class="nav-item">
                <a class="nav-link{{ currentRoute == 'app_movie_index' ? ' active' : '' }}" href="{{ path('app_movie_index') }}">Movie</a>
            </li>
            <li class="nav-item">
                <a class="nav-link{{ currentRoute == 'app_default_contact' ? ' active' : '' }}" href="{{ path('app_default_contact') }}">Contact</a>
            </li>
        </ul>
        <a class="nav-link{{ currentRoute == 'app_register' ? ' active' : '' }}" href="{{ path('app_register') }}">Register</a>
    </div>
</nav>
